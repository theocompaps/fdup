[pytest]
# Custom markers for fdup tests
markers =
    fdup_all: All fdup generated tests (default, small, medium, large)
    slow: Tests that take a long time to run (medium, large suites)

# Default options
addopts = -v

# Test discovery
testpaths = tests
python_files = test_*.py
python_functions = test_*
